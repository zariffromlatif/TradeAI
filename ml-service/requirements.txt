# TradeAI ML service — pin for reproducible installs across machines.
# Python 3.10+ (3.13 supported by these pins).
#
# Install:  pip install -r requirements.txt
# Run:      python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000

fastapi==0.115.6
uvicorn[standard]==0.32.1
# Use a release with manylinux wheels on Render; 2.2.x may fall back to sdist
# and stall on "Preparing metadata" for several minutes (or OOM) on free tiers.
numpy==2.1.4
scikit-learn==1.6.1