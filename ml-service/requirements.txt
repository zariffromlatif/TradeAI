# TradeAI ML service — pin for reproducible installs across machines.
# Python 3.10+ (3.13 supported by these pins).
#
# Install:  pip install -r requirements.txt
# Run:      python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000

fastapi==0.115.6
uvicorn[standard]==0.32.1
# Manylinux wheels for Render/Railway; avoids long sdist builds on free tiers.
# Note: NumPy never published 2.1.4 on PyPI (pip skips 2.1.3 -> 2.2.0).
numpy==2.1.3
scikit-learn==1.6.1